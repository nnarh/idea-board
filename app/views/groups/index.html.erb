<h3>Your Groups</h3>

<ul>
  <% @groups.each do |group| %>
    <li><%= link_to group.name, group %></li>
  <% end %>
</ul>

<ul>
  <%= link_to "Create Group", new_group_path, :class => "button tiny radius round" %>
</ul>
